<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <link rel="stylesheet" href="/styles/login.css">
</head>

<body class="bg-gray-100 min-h-screen flex items-center justify-center p-4">

  <!-- Optional: loader component -->
  <%- include('../components/loader.ejs') %>

  <div class="w-full max-w-md bg-white shadow-md rounded-lg p-8 space-y-6">
    <h3 class="text-2xl font-bold text-center text-gray-700">Login</h3>

    <!-- Flash Messages -->
    <% if (error_msg && error_msg.length > 0) { %>
      <div class="bg-red-100 text-red-700 border border-red-400 rounded p-4 text-sm">
        <%= error_msg %>
      </div>
    <% } %>

    <% if (success_msg && success_msg.length > 0) { %>
      <div class="bg-green-100 text-green-700 border border-green-400 rounded p-4 text-sm">
        <%= success_msg %>
      </div>
    <% } %>

    <form action="/user/login" method="POST" novalidate class="space-y-5">

      <div>
        <label for="username" class="block text-sm font-medium text-gray-600 mb-1">Email</label>
        <input type="email" name="username" placeholder="Enter your Email"
          class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-400" required>
      </div>

      <div>
        <label for="password" class="block text-sm font-medium text-gray-600 mb-1">Password</label>
        <input type="password" name="password" placeholder="Enter Password"
          class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-400" required>
      </div>

      <button type="submit"
        class="w-full bg-blue-600 text-white font-semibold py-2 rounded hover:bg-blue-700 transition duration-200">
        Login
      </button>
      
      <p class="text-sm text-gray-600 text-center">
        Forgot Password? 
        <a href="/user/forget-password" class="text-blue-500 hover:underline font-medium">Reset</a>
      </p>
    </form>
  </div>

</body>
</html>
